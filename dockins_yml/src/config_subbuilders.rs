use crate::config::backend_cfg::BeConfig;
use crate::config::db_cfg::DbConfig;
use crate::config::frontend_cfg::FeConfig;
use crate::config::server_cfg::ServerConfig;

pub fn frontend_cfg_builder(fe_config: &mut FeConfig) {

}

pub fn backend_cfg_builder(be_config: &mut BeConfig) {

}

pub fn server_cfg_builder(server_config: &mut ServerConfig) {

}

pub fn db_cfg_builder(db_config: &mut DbConfig) {

}